export enum Rol {
    Fragger,
    IGL,
    Support,
    Lurker,
    Awper,
}

export enum Actitud {
    Agresivo,
    Controlador,
}

export enum Rango {
    Plata,
    Oro,
    Ak,
    Maestro,
    EliteGlobal,
}